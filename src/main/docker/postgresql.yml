version: '2'
services:
  actioncatalogservice-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/ActionCatalogService/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=ActionCatalogService
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
